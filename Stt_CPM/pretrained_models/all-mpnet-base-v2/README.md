Place the existing Hugging Face `all-mpnet-base-v2` model assets in this directory.

Keep Hugging Face standard filenames unchanged (for example `config.json`,
`tokenizer.json`, `tokenizer_config.json`, and the model weight filename).
Only the parent directory was renamed from `pretrain-model` to `pretrained_models`.
